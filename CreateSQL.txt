CREATE SCHEMA IF NOT EXISTS `hyperefficient` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE `hyperefficient`;

CREATE TABLE IF NOT EXISTS `Categoria` (
  `Id`        INT NOT NULL AUTO_INCREMENT,
  `Nome`      VARCHAR(100) NOT NULL,
  PRIMARY KEY (`Id`)
) ENGINE=InnoDB;

CREATE TABLE IF NOT EXISTS `Setor` (
  `Id`          INT NOT NULL AUTO_INCREMENT,
  `Nome`   VARCHAR(100)   NOT NULL,
  `Descricao`   VARCHAR(300) NULL,
  PRIMARY KEY (`Id`)
) ENGINE=InnoDB;

CREATE TABLE IF NOT EXISTS `Equipamento` (
  `Id`          INT NOT NULL AUTO_INCREMENT,
  `Nome`        VARCHAR(100) NOT NULL,
  `Descricao`   VARCHAR(300) NULL,
  `PotenciaKwh`    DOUBLE NOT NULL,
  `CategoriaId` INT NOT NULL,
  `SetorId`     INT NOT NULL,
  `Ativo`       TINYINT NOT NULL DEFAULT 1,
  `DeviceIdIntegration`   VARCHAR(25) NULL,
  PRIMARY KEY (`Id`),
  INDEX `idx_Equipamento_Categoria` (`CategoriaId`),
  INDEX `idx_Equipamento_Setor`     (`SetorId`),
  CONSTRAINT `fk_Equipamento_Categoria`
      FOREIGN KEY (`CategoriaId`) REFERENCES `Categoria` (`Id`)
      ON DELETE NO ACTION ON UPDATE NO ACTION,
  CONSTRAINT `fk_Equipamento_Setor`
      FOREIGN KEY (`SetorId`)     REFERENCES `Setor`     (`Id`)
      ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB;

CREATE TABLE IF NOT EXISTS `Registro` (
  `Id`            INT NOT NULL AUTO_INCREMENT,
  `DataInicial`   DATETIME NOT NULL,
  `DataFinal`     DATETIME NULL,
  `EquipamentoId` INT NOT NULL,
  `TotalTempo`    DOUBLE GENERATED ALWAYS AS (
    IF(DataFinal IS NOT NULL, TIMESTAMPDIFF(SECOND, DataInicial, DataFinal) / 3600, NULL)
  ) STORED,
  PRIMARY KEY (`Id`),
  INDEX `idx_Registro_Equipamento` (`EquipamentoId`),
  CONSTRAINT `fk_Registro_Equipamento`
      FOREIGN KEY (`EquipamentoId`) REFERENCES `Equipamento` (`Id`)
      ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB;

CREATE TABLE IF NOT EXISTS `Usuario` (
  `Id`       INT NOT NULL AUTO_INCREMENT,
  `Nome`     VARCHAR(100)  NOT NULL,
  `CriadoEm` DATETIME     NOT NULL,
  `Senha`    VARCHAR(255) NOT NULL,
  `Email`    VARCHAR(150) NOT NULL,
  `Ativo`    TINYINT      NOT NULL DEFAULT 1,
  PRIMARY KEY (`Id`)
) ENGINE=InnoDB;